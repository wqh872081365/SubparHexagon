using UnityEngine;
using System.Collections;

public class testScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		this.transform.localScale+=new Vector3(2f,0,0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
